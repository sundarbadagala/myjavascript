// call(), apply(), and bind() are used to control the value of this inside a function.

// call() - Invokes function immediately and passes arguments one by one.

// apply() - same as call() but arguments are passed as array

// bind() Does NOT execute immediately. Returns a new function with fixed this